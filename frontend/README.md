## Poisker Frontend (Flutter)

### Run (Android)
```bash
flutter pub get
flutter run
```

### Run (Web)
```bash
flutter pub get
flutter run -d chrome
```

### Backend base URL
This scaffold uses a simple config constant in `lib/core/config.dart`.
Set it to your backend URL (default: `http://localhost:8000`).
